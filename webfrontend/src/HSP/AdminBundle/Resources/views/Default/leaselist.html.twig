{% extends "HSPPageBundle::layout.html.twig" %}

{% block title "Administration - Leases" %}

{% block content_header 'Administration - Leases' %}
{% block content %}
    <div class="filterForm">
        {{ form(form) }}
    </div>
    <div class="userListTable">
        <table class="userListTable">
            <tr>
                <th>IP(s)</th>
                <th>MAC</th>
                <th>Last seen</th>
            </tr>

            {% for lease in leases %}
                <tr class="{{ cycle(['odd','even'], loop.index0) }}">
                    <td>
                        {{ lease.ipId.ipv4Address }}&nbsp; {{ lease.ipId.ipv6Address }}
                    </td>
                    <td>
                        {{ lease.ipId.macAddress }}
                    </td>
                    <td>
                        {{ lease.ipId.lastseen|date('Y-m-d h:m:s') }}
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}
